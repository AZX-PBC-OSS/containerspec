# API Reference

Auto-generated from docstrings via mkdocstrings. The `containerspec` package
re-exports its full public API (image spec, layers, targets, backends, and
rootfs helpers) through `__all__`, so the single reference below documents every
public class and function.

::: containerspec
